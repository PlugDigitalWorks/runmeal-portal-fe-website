import { MemberQrScreen } from '@/components/loyalty/MemberQrScreen';

/** The counter screen: membership progress + the fixed QR the cashier scans. */
export default function RewardsQrPage() {
    return <MemberQrScreen />;
}
